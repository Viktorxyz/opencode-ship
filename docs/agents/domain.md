# Domain Documentation

This is a single-context repository. Before changing domain behavior, read:

- `CONTEXT.md`, when present;
- relevant decisions under `docs/adr/`.

Missing domain documents are not an error. Create or extend them only when
domain modeling resolves terminology or an architectural decision. Use terms
from `CONTEXT.md` consistently and call out any proposed change that conflicts
with an existing ADR.
